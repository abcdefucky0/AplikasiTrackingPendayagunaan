<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">

    <!-- Warna dalam transparan -->
    <solid android:color="@android:color/transparent" />

    <!-- Border/stroke hitam -->
    <stroke
        android:width="2.5dp"
        android:color="#26823F" />

    <!-- Ukuran circle -->
    <size
        android:width="56dp"
        android:height="56dp" />

</shape>